# Django Backend

Бэкэнд проекта написан с использованием фрэймворка `Django`   

Для панели администратора использовался пакет `django-jet-reboot`

В качестве файлового хранилища используется `minio`   
Для связи с хранилищем `minio` используется `django-storages`

## Схема базы данных

![Scheme](tech-recruiter%20-%20public.png)

*Важные моменты*

* Так как схему базы данных приходилось изменять много раз уже после проектировки некоторые случаи предполагают не самые оптимальные решения.

* Таблицы `orders` и `enrolls` предполагают заявку на покупку курса и запись на курс соответсвенно.

* Таблица `solutions` хранит решения к домашним заданиям отправленные учащимися, сами домашние задания хранятся в таблице `homeworks`