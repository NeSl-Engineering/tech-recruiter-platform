## Nginx conf

Конфигурация nginx лежит в `/nginx/nginx.conf`    
Он настроен так, чтобы и фронт и бэк отдавались с одного адреса и порта.    
Все запросы на сервер будут перенаправляться на фронт, кроме запросов на следующие адреса:
* `/api/*`
* `/admin/`
* `/jet/`
* `/ckeditor5/`
